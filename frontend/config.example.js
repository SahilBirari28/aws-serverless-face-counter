// Copy this file as config.js before running the frontend.
// Never place AWS access keys or secret keys here.

window.APP_CONFIG = {
    API_URL: "https://YOUR-API-ID.execute-api.YOUR-REGION.amazonaws.com"
};
